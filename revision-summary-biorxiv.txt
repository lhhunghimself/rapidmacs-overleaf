Updated the reported timings to RapidMACS v1.0.1 and corrected the software pointers. Peak counts and all output checksums are unchanged, so the byte-identity result and the headline speedup range are unaffected.
